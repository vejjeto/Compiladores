{
  "name": "proyecto-compiladores",
  "version": "1.0.0",
  "description": "Sistema de Control Robótico vía WebSockets",
  "private": true,
  "scripts": {
    "start": "concurrently \"npm start --prefix Backend\" \"npm start --prefix simulador\"",
    "start:backend": "npm start --prefix Backend",
    "install:all": "npm install --prefix Backend && npm install --prefix simulador && npm install",
    "test": "npm test --prefix Backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "git+https://gitlab.com/diego_safar_compiladores/compiladores_diego_safar.git"
  },
  "author": "Diego Safar",
  "license": "ISC"
}
